---
import Base from '../layouts/Base.astro';
const base = import.meta.env.BASE_URL.replace(/\/$/, '');
---
<Base title="Not found">
  <h1>404: Not Found</h1>
  <p>There is no page at this address. Try the <a href={`${base}/`}>front page</a> or the <a href={`${base}/docs/`}>manual</a>.</p>
</Base>
